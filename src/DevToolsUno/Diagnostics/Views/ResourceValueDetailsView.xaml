<UserControl
    x:Class="DevToolsUno.Diagnostics.Views.ResourceValueDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:automation="using:Microsoft.UI.Xaml.Automation"
    xmlns:controls="using:Uno.Controls"
    xmlns:views="using:DevToolsUno.Diagnostics.Views"
    automation:AutomationProperties.Name="Resource value details">
    <Grid x:Name="LayoutRoot"
          MinHeight="0"
          MinWidth="0"
          Padding="{ThemeResource DevToolsPagePadding}"
          RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Spacing="8">
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource DevToolsPageTitleTextStyle}"
                           Text="{Binding SelectedResourceKey}" />
                <TextBlock Style="{StaticResource DevToolsMetaTextStyle}"
                           Text="{Binding SelectedResourceType}" />
            </StackPanel>
            <views:WrapPanel HorizontalSpacing="8"
                             VerticalSpacing="8">
                <Button Command="{Binding CopySelectedPropertyValueCommand}"
                        Content="Copy Property Value"
                        automation:AutomationProperties.Name="Copy selected resource property value" />
            </views:WrapPanel>
        </StackPanel>

        <TabView x:Name="InspectorTabs"
                 Grid.Row="1"
                 MinHeight="0"
                 MinWidth="0"
                 Style="{StaticResource DevToolsEmbeddedTabViewStyle}">
            <TabViewItem Header="Properties">
                <Grid MinHeight="0"
                      MinWidth="0"
                      Padding="0,8,0,0"
                      RowSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBox PlaceholderText="Filter resource properties"
                             Text="{Binding Filter.FilterString, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             automation:AutomationProperties.Name="Filter resource properties" />

                    <Border Grid.Row="1"
                            MinHeight="0"
                            MinWidth="0"
                            Style="{StaticResource DevToolsDataPanelBorderStyle}">
                        <controls:TreeDataGrid x:Name="PropertyGrid"
                                               HorizontalAlignment="Stretch"
                                               VerticalAlignment="Stretch"
                                               Source="{Binding PropertySource}"
                                               automation:AutomationProperties.Name="Resource property grid" />
                    </Border>
                </Grid>
            </TabViewItem>
            <TabViewItem Header="Overview">
                <ScrollViewer MinHeight="0"
                              MinWidth="0"
                              Padding="0,8,0,0"
                              automation:AutomationProperties.Name="Resource metadata">
                    <StackPanel Spacing="10">
                        <Border Style="{StaticResource DevToolsInspectorCardBorderStyle}">
                            <StackPanel Spacing="8">
                                <TextBlock Style="{StaticResource DevToolsSectionTitleTextStyle}" Text="Selected Property" />
                                <ContentControl Content="{Binding SelectedProperty}"
                                                ContentTemplate="{StaticResource SelectedPropertyEditorTemplate}" />
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource DevToolsInspectorCardBorderStyle}">
                            <StackPanel Spacing="4">
                                <TextBlock Style="{StaticResource DevToolsSectionTitleTextStyle}"
                                           Text="Resource" />
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundCaptionBrush}" Text="Value:" />
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundMutedBrush}" Text="{Binding SelectedResourceValue}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundCaptionBrush}" Text="Provider:" />
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundMutedBrush}" Text="{Binding ProviderName}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundCaptionBrush}" Text="Provider Kind:" />
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundMutedBrush}" Text="{Binding ProviderKind}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundCaptionBrush}" Text="Path:" />
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundMutedBrush}" Text="{Binding ProviderPath}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Spacing="4">
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundCaptionBrush}" Text="Dictionary:" />
                                    <TextBlock Foreground="{ThemeResource DevToolsForegroundMutedBrush}" Text="{Binding DictionarySource}" TextWrapping="WrapWholeWords" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </TabViewItem>
            <TabViewItem Header="Sources">
                <Grid MinHeight="0"
                      MinWidth="0"
                      Padding="0,8,0,0">
                    <Border MinHeight="0"
                            MinWidth="0"
                            Style="{StaticResource DevToolsDataPanelBorderStyle}">
                        <controls:TreeDataGrid Source="{Binding ValueSourceGrid}"
                                               HorizontalAlignment="Stretch"
                                               VerticalAlignment="Stretch"
                                               automation:AutomationProperties.Name="Resource property sources" />
                    </Border>
                </Grid>
            </TabViewItem>
        </TabView>
    </Grid>
</UserControl>
